% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propensity_diagnostics.R
\name{propensity_diagnostics}
\alias{propensity_diagnostics}
\title{Propensity Score Diagnostics Panel}
\usage{
propensity_diagnostics(
  ps_scores,
  treatment,
  data = NULL,
  method = c("logit", "rf", "any"),
  title = NULL
)
}
\arguments{
\item{ps_scores}{Numeric vector of estimated propensity scores (values in
(0, 1)).}

\item{treatment}{Integer or logical vector of the same length as
\code{ps_scores}. Treatment indicator (1 = treated, 0 = control).}

\item{data}{Data frame or \code{NULL}. If supplied, it is used only for
labelling purposes.}

\item{method}{Character. Label for the propensity score estimation method
shown in plot titles (e.g. \code{"logit"}, \code{"rf"}). Default \code{"any"}.}

\item{title}{Character or \code{NULL}. Optional overall title prefix.}
}
\value{
A named list:
\describe{
\item{\code{overlap_plot}}{ggplot2 density overlap plot.}
\item{\code{histogram_plot}}{ggplot2 histogram by treatment group.}
\item{\code{calibration_plot}}{ggplot2 calibration plot.}
\item{\code{combined_plot}}{Patchwork combined figure (or \code{overlap_plot}
if \pkg{patchwork} is unavailable).}
\item{\code{overlap_stats}}{Named numeric vector: \code{min_treated},
\code{max_control}, \code{common_support_lo}, \code{common_support_hi},
\code{pct_off_support}.}
}
}
\description{
Evaluates overlap, common support, and calibration of
estimated propensity scores. All three plots are returned individually
and as a combined figure.
}
\details{
Comprehensive Propensity Score Diagnostics

Produces a set of diagnostic plots and statistics for evaluating a
propensity score model: (1) an overlap density plot, (2) a histogram by
treatment group with trimming bounds, and (3) a calibration plot comparing
predicted treatment probability with observed treatment rate by decile.
The three panels are assembled into a combined figure via \pkg{patchwork}
if available.
}
\examples{
set.seed(42)
n   <- 400
x1  <- rnorm(n)
x2  <- rnorm(n)
lp  <- -0.5 + 0.8 * x1 + 0.5 * x2
ps  <- 1 / (1 + exp(-lp))
trt <- rbinom(n, 1, ps)

diag <- propensity_diagnostics(ps_scores = ps, treatment = trt)
diag$combined_plot
diag$overlap_stats

}
