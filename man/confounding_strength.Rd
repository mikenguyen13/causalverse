% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confounding_strength.R
\name{confounding_strength}
\alias{confounding_strength}
\title{Sensitivity Analysis for Omitted Variable Bias (E-value & RV)}
\usage{
confounding_strength(
  estimate,
  se,
  df = Inf,
  benchmark_covariates = NULL,
  data = NULL,
  outcome = NULL,
  treatment = NULL,
  alpha = 0.05
)
}
\arguments{
\item{estimate}{Numeric. The point estimate of the causal effect (from any
model).}

\item{se}{Numeric. The standard error of the estimate.}

\item{df}{Numeric. Residual degrees of freedom. Use \code{Inf} for large samples.
Default \code{Inf}.}

\item{benchmark_covariates}{Character vector or \code{NULL}. Names of observed
covariates to use as benchmarks (requires \code{data}, \code{outcome}, and
\code{treatment}). Optional.}

\item{data}{Data frame or \code{NULL}. Required when \code{benchmark_covariates} is
specified.}

\item{outcome}{Character or \code{NULL}. Outcome variable name in \code{data}.}

\item{treatment}{Character or \code{NULL}. Treatment variable name in \code{data}.}

\item{alpha}{Numeric. Significance level for the CI-based E-value. Default
\code{0.05}.}
}
\value{
A named list:
\describe{
\item{\code{e_value}}{Named numeric: \code{e_value} (for the point estimate) and
\code{e_value_ci} (for the confidence limit closer to the null).}
\item{\code{robustness_value}}{Named numeric: \code{rv_q0} (to explain away the
point estimate) and \code{rv_qa} (to explain away the CI bound).}
\item{\code{sensitivity_df}}{Data frame of bias-adjusted estimates across a
grid of \code{r2dz} (0–0.5) × \code{r2yz} (0–0.5) partial R² values.}
\item{\code{plot}}{A ggplot2 contour plot showing the sensitivity surface.}
}
}
\description{
Implements the E-value of VanderWeele & Ding (2017) and the
robustness value / bias formula of Cinelli & Hazlett (2020) to quantify
how much unmeasured confounding would be needed to explain away a causal
estimate.
}
\details{
Sensitivity Analysis for Omitted Variable Bias

Computes sensitivity measures for assessing robustness of a causal
estimate to unobserved confounding:
\itemize{
\item \strong{E-value} (VanderWeele & Ding 2017): the minimum strength of
association (on a risk-ratio scale) that an unmeasured confounder
would need with \emph{both} the treatment and the outcome to fully explain
away the observed effect.
\item \strong{Robustness value} (Cinelli & Hazlett 2020): the minimum partial
R² that the confounder would need to explain in both the treatment and
the outcome to drive the estimate to zero (or the null).
\item \strong{Bias-adjusted contour plot}: estimated effect across a grid of
partial R² values for the confounder's association with treatment
(r²[dz|x]) and outcome (r²[yz|x]).
}
No dependency on \pkg{sensemakr} is required; all computations use base R.
}
\examples{
# Simulated linear regression
set.seed(42)
n  <- 500
x  <- rnorm(n)
y  <- 0.4 * x + rnorm(n)
m  <- lm(y ~ x)
sm <- summary(m)

est <- coef(m)["x"]
se  <- coef(sm)["x", "Std. Error"]
df_r <- m$df.residual

sens <- confounding_strength(estimate = est, se = se, df = df_r)
print(sens$e_value)
print(sens$robustness_value)
sens$plot

}
\references{
VanderWeele, T. J., & Ding, P. (2017). Sensitivity analysis in
observational research: Introducing the E-value. \emph{Annals of Internal
Medicine}, 167(4), 268-274.

Cinelli, C., & Hazlett, C. (2020). Making sense of sensitivity: Extending
omitted variable bias. \emph{Journal of the Royal Statistical Society: Series B},
82(1), 39-67.
}
