% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_summary.R
\name{causal_summary}
\alias{causal_summary}
\title{Unified Causal Model Summary}
\usage{
causal_summary(model, method_name = NULL, digits = 3, conf_level = 0.95, ...)
}
\arguments{
\item{model}{A fitted model object. Supported: \code{lm}, \code{glm}, \code{fixest},
\code{ivreg}, and any model class with a \code{summary()} method that returns a
coefficient table.}

\item{method_name}{Character or \code{NULL}. Label for the estimation method.
If \code{NULL}, the model class is used.}

\item{digits}{Integer. Number of decimal places for display. Default \code{3}.}

\item{conf_level}{Numeric. Confidence level for the interval. Default \code{0.95}.}

\item{...}{Additional arguments passed to \code{broom::tidy()} if available.}
}
\value{
An object of class \code{"causal_summary"} inheriting from \code{"data.frame"}.
Columns: \code{method}, \code{term}, \code{estimate}, \code{std_error}, \code{ci_lower},
\code{ci_upper}, \code{p_value}, \code{n_obs}, \code{outcome}.
}
\description{
Returns a clean one-row data frame with the key causal
estimate, standard error, confidence interval, p-value, number of
observations, and outcome variable name. This is useful for programmatic
comparison of many models.
}
\details{
One-Line Tidy Summary for Any Causal Model

Extracts a standardised single-row data frame from any causal inference
model (lm, glm, fixest, ivreg, etc.). Works via \pkg{broom} if available,
with manual fallback extraction for common model classes. A custom S3
print method provides a clean, console-friendly summary.
}
\examples{
set.seed(1)
n  <- 300
x  <- rnorm(n)
y  <- 2 * x + rnorm(n)
df <- data.frame(y = y, x = x)

m_lm <- lm(y ~ x, data = df)
cs   <- causal_summary(m_lm, method_name = "OLS")
print(cs)

if (requireNamespace("fixest", quietly = TRUE)) {
  m_fe <- fixest::feols(y ~ x, data = df)
  cs2  <- causal_summary(m_fe, method_name = "OLS (fixest)")
  print(cs2)
}

}
