% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stacked_did.R
\name{stacked_did}
\alias{stacked_did}
\title{Stacked Difference-in-Differences}
\usage{
stacked_did(
  data,
  unit_var,
  time_var,
  outcome_var,
  treat_time_var,
  anticipation = 0,
  clean_controls = TRUE,
  plot = TRUE
)
}
\arguments{
\item{data}{A data frame in balanced long panel format.}

\item{unit_var}{Character. Name of the unit/panel ID column.}

\item{time_var}{Character. Name of the time period column (integer/numeric).}

\item{outcome_var}{Character. Name of the outcome variable column.}

\item{treat_time_var}{Character. Name of the column recording each unit's
\strong{first} treatment period (\code{NA} or a large number for never-treated).}

\item{anticipation}{Integer. Number of pre-treatment periods to exclude from
the "clean" window. Default \code{0}.}

\item{clean_controls}{Logical. If \code{TRUE} (default), exclude already-treated
units from the control group within each sub-experiment.}

\item{plot}{Logical. Whether to return an event-study plot. Default \code{TRUE}.}
}
\value{
A named list:
\describe{
\item{\code{estimate}}{Scalar ATT (weighted average across cohorts).}
\item{\code{event_study}}{Data frame with columns \code{rel_period}, \code{estimate},
\code{ci_lo}, \code{ci_hi}.}
\item{\code{stacked_data}}{The stacked data frame used for estimation.}
\item{\code{plot}}{A ggplot2 event-study plot, or \code{NULL} if \code{plot = FALSE}.}
}
}
\description{
Builds the stacked dataset described in Baker et al. (2022)
and estimates TWFE regressions to recover the ATT and dynamic effects
free of contamination from heterogeneous treatment timing.
}
\details{
Stacked Difference-in-Differences Estimator

Implements the stacked DiD estimator following Cengiz et al. (2019) and
Baker et al. (2022). For each treatment cohort, a "sub-experiment" dataset
is created by stacking the cohort's treated units with clean control units
(never-treated or not-yet-treated). TWFE is then estimated on this stacked
dataset with cohort-by-period interactions, returning an average treatment
effect on the treated (ATT) and event-study dynamic effects.
}
\examples{
if (requireNamespace("fixest", quietly = TRUE)) {
  data("base_stagg", package = "fixest")
  # base_stagg has: id, year, treated, treatment_time, y, ...
  res <- stacked_did(
    data           = base_stagg,
    unit_var       = "id",
    time_var       = "year",
    outcome_var    = "y",
    treat_time_var = "year_treated",
    anticipation   = 0,
    clean_controls = TRUE,
    plot           = TRUE
  )
  print(res$estimate)
  res$plot
}

}
\references{
Baker, A. C., Larcker, D. F., & Wang, C. C. Y. (2022). How much should we
trust staggered difference-in-differences estimates? \emph{Journal of Financial
Economics}, 144(2), 370-395.

Cengiz, D., Dube, A., Lindner, A., & Zipperer, B. (2019). The effect of
minimum wages on low-wage jobs. \emph{Quarterly Journal of Economics}, 134(3),
1405-1454.
}
