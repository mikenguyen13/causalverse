% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_table.R
\name{causal_table}
\alias{causal_table}
\title{Publication-Ready Causal Inference Results Table}
\usage{
causal_table(
  models,
  coef_map = NULL,
  gof_rows = c("nobs", "r.squared", "adj.r.squared"),
  title = "Causal Inference Results",
  notes = NULL,
  stars = c(`*` = 0.1, `**` = 0.05, `***` = 0.01),
  output_format = c("html", "latex", "dataframe"),
  digits = 3
)
}
\arguments{
\item{models}{Named list of model objects or tidy data frames (from
\code{tidy_causal()}).}

\item{coef_map}{Named character vector. Mapping from coefficient names
to display names. E.g., \code{c(treat = "Treatment", age = "Age")}.}

\item{gof_rows}{Character vector. Goodness-of-fit statistics to include.
Default \code{c("nobs", "r.squared", "adj.r.squared")}.}

\item{title}{Character. Table title.}

\item{notes}{Character or \code{NULL}. Table footnote.}

\item{stars}{Logical or named numeric vector. Significance thresholds.
Default \code{c("*" = 0.1, "**" = 0.05, "***" = 0.01)}.}

\item{output_format}{Character. \code{"html"} (default), \code{"latex"},
or \code{"dataframe"}.}

\item{digits}{Integer. Decimal places. Default \code{3}.}
}
\value{
A \pkg{modelsummary} table, \pkg{gt} table, or data frame.
}
\description{
Produces a formatted regression table comparing multiple causal inference
estimates, suitable for journal submission. Supports HTML, LaTeX, and
plain text output via \pkg{modelsummary} or a built-in fallback.
}
\examples{
\dontrun{
m1 <- lm(mpg ~ am, data = mtcars)
m2 <- lm(mpg ~ am + wt, data = mtcars)
m3 <- lm(mpg ~ am + wt + hp, data = mtcars)

causal_table(
  models   = list("(1) Bivariate" = m1, "(2) + Weight" = m2,
                  "(3) Full" = m3),
  coef_map = c(am = "Manual Transmission", wt = "Weight (1000 lbs)",
               hp = "Horsepower"),
  title    = "Effect of Transmission Type on MPG"
)
}

}
