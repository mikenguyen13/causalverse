% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dose_response.R
\name{dose_response}
\alias{dose_response}
\title{Dose-Response Estimation for Continuous Treatments}
\usage{
dose_response(
  data,
  outcome,
  treatment,
  covariates = NULL,
  method = c("gps", "regression", "loess"),
  n_grid = 50,
  poly_degree = 2,
  conf_level = 0.95,
  boot_reps = 200,
  seed = 42,
  trim_percentile = 0.01,
  plot = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome}{Character. Outcome variable name.}

\item{treatment}{Character. Continuous treatment variable name.}

\item{covariates}{Character vector. Covariate names for GPS model.}

\item{method}{Character. Estimation method: \code{"gps"} (generalized
propensity score, default), \code{"regression"} (polynomial OLS),
or \code{"loess"} (nonparametric LOESS).}

\item{n_grid}{Integer. Number of grid points for the dose-response curve.
Default \code{50}.}

\item{poly_degree}{Integer. Polynomial degree for GPS or regression
methods. Default \code{2}.}

\item{conf_level}{Numeric. Confidence level. Default \code{0.95}.}

\item{boot_reps}{Integer. Bootstrap replicates for CI. Default \code{200}.}

\item{seed}{Integer. Random seed. Default \code{42}.}

\item{trim_percentile}{Numeric. Trim extreme treatment values at this
percentile (both tails). Default \code{0.01}.}

\item{plot}{Logical. Return a dose-response plot. Default \code{TRUE}.}
}
\value{
A list with:
\describe{
\item{dose_response}{Data frame: dose grid, estimated outcome,
SE, CI bounds.}
\item{ape}{Numeric. Average partial effect (slope at mean dose).}
\item{plot}{ggplot2 dose-response curve.}
}
}
\description{
Estimates the dose-response function (average potential outcome as a
function of treatment dose) for continuous treatments using a
generalized propensity score (GPS) approach or regression-based
methods. Produces smooth dose-response curves with confidence bands.
}
\examples{
set.seed(42)
n <- 500
df <- data.frame(
  X1 = rnorm(n), X2 = rnorm(n),
  D  = runif(n, 0, 10)
)
df$D  <- df$D + df$X1  # confounded dose
df$Y  <- 0.5 * df$D - 0.03 * df$D^2 + df$X1 + rnorm(n)

result <- dose_response(
  data       = df,
  outcome    = "Y",
  treatment  = "D",
  covariates = c("X1", "X2"),
  method     = "gps"
)
result$plot

}
\references{
Hirano, K., & Imbens, G. W. (2004). The propensity score with
continuous treatments. In A. Gelman & X.-L. Meng (Eds.),
\emph{Applied Bayesian Modeling and Causal Inference from Incomplete-
Data Perspectives} (pp. 73–84). Wiley.
}
