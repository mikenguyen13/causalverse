% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placebo_test.R
\name{placebo_test}
\alias{placebo_test}
\title{Randomization Inference and Placebo Tests for Treatment Effects}
\usage{
placebo_test(
  data,
  outcome,
  treatment,
  estimator = "ate",
  unit_var = NULL,
  time_var = NULL,
  post_var = NULL,
  n_perms = 1000,
  test_stat = c("mean", "t"),
  two_sided = TRUE,
  seed = 42,
  plot = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome}{Character. Name of the outcome variable.}

\item{treatment}{Character. Name of the binary treatment indicator.}

\item{estimator}{Function or character. The estimator to use:
\code{"ate"} (default, difference in means), \code{"did"}
(difference-in-differences requires \code{time_var}), or a custom
function \code{f(data, outcome, treatment, ...)} returning a scalar.}

\item{unit_var}{Character. Unit identifier (for panel/DiD). Default
\code{NULL}.}

\item{time_var}{Character. Time period variable (for DiD). Default
\code{NULL}.}

\item{post_var}{Character. Binary indicator for post-treatment period
(for DiD). Default \code{NULL}.}

\item{n_perms}{Integer. Number of permutations. Default \code{1000}.}

\item{test_stat}{Character. Test statistic: \code{"mean"} (difference in
means) or \code{"t"} (t-statistic). Default \code{"mean"}.}

\item{two_sided}{Logical. Two-sided p-value. Default \code{TRUE}.}

\item{seed}{Integer. Random seed. Default \code{42}.}

\item{plot}{Logical. Whether to plot the permutation distribution. Default
\code{TRUE}.}
}
\value{
A list with:
\describe{
\item{observed_stat}{Numeric. Observed test statistic.}
\item{p_value}{Numeric. Randomization inference p-value.}
\item{perm_dist}{Numeric vector of permuted test statistics.}
\item{plot}{ggplot2 histogram of the permutation distribution.}
}
}
\description{
Conducts Fisher's randomization inference (permutation test) for causal
effects, computing sharp null p-values and visualizing the randomization
distribution. Also supports in-time and in-space placebo tests.
}
\examples{
set.seed(42)
df <- data.frame(
  Y = c(rnorm(50, mean = 0.5), rnorm(50, mean = 0)),
  D = c(rep(1, 50), rep(0, 50))
)
result <- placebo_test(data = df, outcome = "Y", treatment = "D")
result$p_value
result$plot

}
\references{
Fisher, R. A. (1935). \emph{The Design of Experiments}. Oliver & Boyd.

Imbens, G. W., & Rubin, D. B. (2015). \emph{Causal Inference for Statistics,
Social, and Biomedical Sciences}. Cambridge University Press.
}
