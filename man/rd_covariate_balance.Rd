% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd_covariate_balance.R
\name{rd_covariate_balance}
\alias{rd_covariate_balance}
\title{RD Covariate Balance Test}
\usage{
rd_covariate_balance(
  data,
  covariates,
  x,
  c = 0,
  bw = NULL,
  kernel = "tri",
  p = 1,
  conf_level = 0.95,
  theme_use = causalverse::ama_theme()
)
}
\arguments{
\item{data}{A data frame containing the covariates and running variable.}

\item{covariates}{Character vector. Names of covariate columns to test.}

\item{x}{Character string. Name of the running variable.}

\item{c}{Numeric. The cutoff value for the RD design. Default is \code{0}.}

\item{bw}{Numeric or \code{NULL}. Bandwidth to use for all tests. If
\code{NULL} (the default), \code{rdrobust} selects the MSE-optimal
bandwidth separately for each covariate.}

\item{kernel}{Character string. Kernel function for \code{rdrobust}.
Default is \code{"tri"} (triangular).}

\item{p}{Integer. Order of the local polynomial. Default is \code{1}.}

\item{conf_level}{Numeric. Confidence level for intervals. Default is
\code{0.95}.}

\item{theme_use}{A ggplot2 theme to apply to the plot. Default is
\code{causalverse::ama_theme()}.}
}
\value{
A list with components:
\describe{
\item{\code{table}}{A data frame with columns: \code{covariate},
\code{estimate}, \code{std_error}, \code{ci_lower}, \code{ci_upper},
\code{p_value}, \code{bandwidth}, \code{n_left}, \code{n_right},
\code{significant}.}
\item{\code{plot}}{A ggplot2 coefficient plot showing the estimate and
CI for each covariate.}
}
}
\description{
Tests covariate balance at the RD cutoff by running
\code{\link[rdrobust]{rdrobust}} with each covariate as the outcome
variable. If the RD design is valid, predetermined covariates should not
exhibit a discontinuity at the cutoff.
}
\examples{
\dontrun{
set.seed(42)
n <- 1000
x <- runif(n, -1, 1)
df <- data.frame(
  x   = x,
  age = 30 + 5 * x + rnorm(n),
  inc = 50000 + 10000 * x + rnorm(n, sd = 5000),
  edu = 12 + 2 * x + rnorm(n, sd = 1.5)
)

result <- rd_covariate_balance(
  data       = df,
  covariates = c("age", "inc", "edu"),
  x          = "x",
  c          = 0
)

result$plot
result$table
}

}
\references{
Lee, D. S. and Lemieux, T. (2010). "Regression Discontinuity Designs in
Economics." \emph{Journal of Economic Literature}, 48(2), 281-355.
}
