% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coef_plot.R
\name{coef_plot}
\alias{coef_plot}
\title{Universal Coefficient Plot}
\usage{
coef_plot(
  models,
  terms = NULL,
  term_labels = NULL,
  model_labels = NULL,
  sort_by = c("estimate", "name", "none"),
  add_zero_line = TRUE,
  color_by = c("model", "significance"),
  conf_level = 0.95,
  dodge_width = 0.4,
  title = NULL,
  xlab = "Estimate (95 pct CI)"
)
}
\arguments{
\item{models}{A model object, a named list of model objects, or a tidy
data frame with columns \code{term}, \code{estimate}, \code{conf.low},
\code{conf.high} (broom-style) or \code{ci_lower}, \code{ci_upper}
(causalverse-style).}

\item{terms}{Character vector. Terms to include. If \code{NULL} (default),
all non-intercept terms are shown.}

\item{term_labels}{Named character vector mapping original term names to
display labels, e.g. \code{c(am = "Transmission (auto)")}.}

\item{model_labels}{Character vector. Display names for each model, used in
the legend. Defaults to list element names or \code{"Model"}.}

\item{sort_by}{Character. Sort terms by \code{"estimate"} (default),
\code{"name"}, or \code{"none"}.}

\item{add_zero_line}{Logical. Add a vertical reference line at zero.
Default \code{TRUE}.}

\item{color_by}{Character. Color points by \code{"model"} (default) or
\code{"significance"} (significant at 5 percent in a different color).}

\item{conf_level}{Numeric. Confidence level for intervals extracted from
model objects. Default \code{0.95}.}

\item{dodge_width}{Numeric. Horizontal offset between multiple models.
Default \code{0.4}.}

\item{title}{Character or \code{NULL}. Plot title.}

\item{xlab}{Character. X-axis label. Default \code{"Estimate (95 pct CI)"}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Creates a publication-ready coefficient (forest) plot from any model or
list of models. Supports \code{lm}, \code{glm}, \code{fixest},
\code{ivreg}, and tidy data frames.
}
\examples{
# Single lm model
mod <- lm(mpg ~ am + wt + hp, data = mtcars)
coef_plot(mod, terms = c("am", "wt", "hp"),
          term_labels = c(am = "Transmission (auto)",
                          wt = "Weight", hp = "Horsepower"))

\dontrun{
# Multiple models
mod2 <- lm(mpg ~ am + wt + hp + disp, data = mtcars)
coef_plot(
  list(Parsimonious = mod, Full = mod2),
  terms = c("am", "wt", "hp"),
  color_by = "significance"
)
}

}
