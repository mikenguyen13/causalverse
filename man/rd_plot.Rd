% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression_discontinuity_plot.R
\name{rd_plot}
\alias{rd_plot}
\title{Publication-Ready Regression Discontinuity Plot}
\usage{
rd_plot(
  y,
  x,
  cutoff = 0,
  nbins = NULL,
  poly_order = 4L,
  ci_shade = TRUE,
  title = NULL,
  xlab = "Running Variable",
  ylab = "Outcome",
  cutoff_color = "red",
  color_sides = TRUE,
  rdplot_args = list()
)
}
\arguments{
\item{y}{Numeric vector. Outcome variable.}

\item{x}{Numeric vector. Running variable. Values are shifted internally so
the cutoff is at zero.}

\item{cutoff}{Numeric. RD cutoff value on the original scale. Default
\code{0}.}

\item{nbins}{Integer or \code{NULL}. Number of bins per side. If \code{NULL}
(default), MSE-optimal binning from \code{rdrobust::rdplot} is used.}

\item{poly_order}{Integer. Polynomial order for the smooth fit lines.
Default \code{4}.}

\item{ci_shade}{Logical. Add confidence band shading around the polynomial
fit. Default \code{TRUE}.}

\item{title}{Character or \code{NULL}. Plot title.}

\item{xlab}{Character. X-axis label. Default \code{"Running Variable"}.}

\item{ylab}{Character. Y-axis label. Default \code{"Outcome"}.}

\item{cutoff_color}{Character. Color of the vertical cutoff line.
Default \code{"red"}.}

\item{color_sides}{Logical. Use different colors for the control (left) and
treated (right) sides. Default \code{TRUE}.}

\item{rdplot_args}{List. Additional named arguments forwarded to
\code{rdrobust::rdplot} (ignored when \pkg{rdrobust} is not available).}
}
\value{
A \code{ggplot2} object.
}
\description{
Creates a publication-ready RD plot with optimal binning, local polynomial
fit, confidence bands, and a cutoff line. Wraps \code{rdrobust::rdplot}
from the \pkg{rdrobust} package with enhanced \pkg{ggplot2} aesthetics. When
\pkg{rdrobust} is not installed a simple local polynomial via \code{lm} is
used instead.
}
\examples{
\dontrun{
library(rdrobust)
data(rdrobust_RDsenate)
rd_plot(
  y      = rdrobust_RDsenate$vote,
  x      = rdrobust_RDsenate$margin,
  cutoff = 0,
  title  = "RD: Senate Vote Share",
  xlab   = "Democratic Vote Share Margin",
  ylab   = "Democratic Vote Share (next election)"
)
}

}
\references{
Calonico, S., Cattaneo, M. D., and Titiunik, R. (2015). "rdrobust: An R
Package for Robust Nonparametric Inference in Regression-Discontinuity
Designs." \emph{R Journal}, 7(1), 38-51.
}
