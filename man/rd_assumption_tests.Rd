% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd_assumption_tests.R
\name{rd_assumption_tests}
\alias{rd_assumption_tests}
\title{Comprehensive RD Assumption Tests}
\usage{
rd_assumption_tests(
  data,
  outcome,
  running_var,
  cutoff = 0,
  covariates = NULL,
  bw_seq = c(0.5, 0.75, 1, 1.25, 1.5),
  placebo_cutoffs = NULL,
  poly_orders = 1:4,
  kernel = "triangular",
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome}{Character. Outcome variable name.}

\item{running_var}{Character. Running (forcing) variable name.}

\item{cutoff}{Numeric. RD cutoff. Default \code{0}.}

\item{covariates}{Character vector or \code{NULL}. Covariates to test for
balance at the cutoff.}

\item{bw_seq}{Numeric vector. Bandwidth multipliers to test in sensitivity
analysis. Default \code{c(0.5, 0.75, 1, 1.25, 1.5)}.}

\item{placebo_cutoffs}{Numeric vector or \code{NULL}. Placebo cutoff values
(should be far from \code{cutoff}). If \code{NULL}, auto-selected.}

\item{poly_orders}{Integer vector. Polynomial orders to test. Default
\code{1:4}.}

\item{kernel}{Character. RD kernel. Default \code{"triangular"}.}

\item{verbose}{Logical. Whether to print test results. Default \code{TRUE}.}
}
\value{
A list with:
\describe{
\item{density_test}{Output from \code{rddensity::rddensity()}.}
\item{covariate_balance}{Data frame of RD estimates on each covariate.}
\item{bandwidth_sensitivity}{Data frame of estimates at different
bandwidths.}
\item{placebo_results}{Data frame of placebo cutoff estimates.}
\item{poly_sensitivity}{Data frame of estimates by polynomial order.}
\item{summary_report}{Character. Text summary of all tests.}
}
}
\description{
Runs a battery of standard robustness and assumption tests for regression
discontinuity designs: density continuity (McCrary/rddensity), covariate
balance at the cutoff, bandwidth sensitivity, placebo cutoffs, and
polynomial order sensitivity.
}
\examples{
\dontrun{
library(rdrobust)
data(rdrobust_RDsenate)
result <- rd_assumption_tests(
  data        = rdrobust_RDsenate,
  outcome     = "vote",
  running_var = "margin"
)
cat(result$summary_report)
}

}
