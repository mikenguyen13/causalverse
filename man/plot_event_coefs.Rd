% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_event_coefs.R
\name{plot_event_coefs}
\alias{plot_event_coefs}
\title{Plot Event Study Coefficients from Multiple Estimators}
\usage{
plot_event_coefs(
  estimates,
  reference_period = -1,
  conf_level = 0.95,
  title = NULL,
  xlab = "Periods Relative to Treatment",
  ylab = "Estimate"
)
}
\arguments{
\item{estimates}{A named list of data frames, each with columns
\code{term} (relative time period), \code{estimate}, and \code{std_error}.
The names of the list become the legend labels.}

\item{reference_period}{Numeric. The reference period (omitted category).
Default is \code{-1}.}

\item{conf_level}{Numeric. Confidence level for error bars. Default is 0.95.}

\item{title}{Character string. Plot title. Default is \code{NULL}.}

\item{xlab}{Character string. X-axis label. Default is \code{"Periods Relative to Treatment"}.}

\item{ylab}{Character string. Y-axis label. Default is \code{"Estimate"}.}
}
\value{
A ggplot2 object.
}
\description{
Creates a combined coefficient plot comparing event study estimates from
multiple estimators (e.g., TWFE, Sun-Abraham, Callaway-Sant'Anna, Gardner).
This is useful for showing robustness of dynamic treatment effects across
different estimation strategies.
}
\examples{
\dontrun{
# After running different estimators:
estimates <- list(
  "TWFE" = data.frame(term = -5:5, estimate = rnorm(11), std_error = 0.1),
  "Sun-Abraham" = data.frame(term = -5:5, estimate = rnorm(11), std_error = 0.1)
)
plot_event_coefs(estimates)
}

}
