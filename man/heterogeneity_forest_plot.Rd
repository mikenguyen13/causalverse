% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heterogeneity_forest_plot.R
\name{heterogeneity_forest_plot}
\alias{heterogeneity_forest_plot}
\title{Heterogeneity Forest Plot}
\usage{
heterogeneity_forest_plot(
  estimates,
  subgroup = NULL,
  overall = NULL,
  xlab = "Estimated Effect",
  title = "Subgroup Analysis",
  ref_line = 0,
  sort_by = c("estimate", "none")
)
}
\arguments{
\item{estimates}{A data frame with at minimum these columns:
\describe{
\item{\code{label}}{Character. Subgroup label.}
\item{\code{estimate}}{Numeric. Point estimate.}
\item{\code{ci_lower}}{Numeric. Lower confidence interval bound.}
\item{\code{ci_upper}}{Numeric. Upper confidence interval bound.}
}
Optional columns: \code{n} (sample size, shown as text), \code{p_value}
(used for significance colouring).}

\item{subgroup}{Character or \code{NULL}. Column name in \code{estimates} to use as
a grouping factor for colouring. Default \code{NULL}.}

\item{overall}{A single-row data frame with columns \code{estimate},
\code{ci_lower}, \code{ci_upper} for the overall effect diamond. \code{NULL} to omit.}

\item{xlab}{Character. x-axis label. Default \code{"Estimated Effect"}.}

\item{title}{Character. Plot title. Default \code{"Subgroup Analysis"}.}

\item{ref_line}{Numeric. x-intercept for the reference line. Default \code{0}.}

\item{sort_by}{Character. \code{"estimate"} to sort subgroups by effect size,
\code{"none"} to preserve input order. Default \code{"estimate"}.}
}
\value{
A ggplot2 object.
}
\description{
Accepts a data frame of subgroup estimates and produces a
forest plot suitable for publication. Subgroups can be sorted by
effect size. An optional overall estimate is shown as a diamond.
}
\details{
Publication-Ready Forest Plot for Subgroup / CATE Analysis

Creates a publication-ready forest plot visualising treatment effect
heterogeneity across subgroups or conditional average treatment effects
(CATE). Confidence intervals are shown as horizontal whiskers, the point
estimate as a filled dot colour-coded by statistical significance, and an
optional overall-effect diamond is drawn at the bottom.
}
\examples{
est_df <- data.frame(
  label    = c("Female", "Male", "Age < 40", "Age >= 40",
               "Low income", "High income"),
  estimate = c(0.18, 0.09, 0.22, 0.07, 0.25, 0.05),
  ci_lower = c(0.08, -0.01, 0.11, -0.04, 0.13, -0.06),
  ci_upper = c(0.28, 0.19, 0.33, 0.18, 0.37, 0.16),
  n        = c(150, 150, 140, 160, 130, 170),
  p_value  = c(0.001, 0.08, 0.001, 0.26, 0.001, 0.40),
  stringsAsFactors = FALSE
)

overall_df <- data.frame(estimate = 0.13, ci_lower = 0.07, ci_upper = 0.19)

heterogeneity_forest_plot(
  estimates = est_df,
  overall   = overall_df,
  title     = "Subgroup Treatment Effects",
  sort_by   = "estimate"
)

}
