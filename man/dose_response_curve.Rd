% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dose_response_curve.R
\name{dose_response_curve}
\alias{dose_response_curve}
\title{Dose-Response Curve for Continuous Treatment}
\usage{
dose_response_curve(
  data,
  outcome,
  treatment,
  covariates = NULL,
  n_bins = 20,
  method = c("ipw", "ols", "gam"),
  plot = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome}{Character. Name of the continuous outcome variable.}

\item{treatment}{Character. Name of the continuous treatment variable.}

\item{covariates}{Character vector. Names of pre-treatment covariates to
adjust for. \code{NULL} for unadjusted estimates.}

\item{n_bins}{Integer. Number of bins for the treatment axis. Default \code{20}.}

\item{method}{Character. Estimation method: \code{"ipw"} (generalised propensity
score weighting), \code{"ols"} (binned local means), or \code{"gam"} (GAM via
\pkg{mgcv}, if available). Default \code{"ipw"}.}

\item{plot}{Logical. Whether to print and return the plot. Default \code{TRUE}.}
}
\value{
A named list:
\describe{
\item{\code{curve_df}}{Data frame with columns \code{dose_mid}, \code{estimate},
\code{ci_lo}, \code{ci_hi}, \code{n} (observations in each bin).}
\item{\code{plot}}{A ggplot2 object, or \code{NULL} if \code{plot = FALSE}.}
}
}
\description{
Estimates the dose-response function for a continuous treatment
using one of three methods and plots the resulting curve with a shaded
95\% CI ribbon. The IPW approach follows Hirano & Imbens (2004).
}
\details{
Dose-Response Curve Estimation for Continuous Treatments

Estimates and visualises the average potential outcome E[Y(d)] as a
function of a continuous treatment dose \emph{d}. Three estimation strategies
are available: inverse-probability weighting with a generalised propensity
score (IPW), binned OLS, and a generalised additive model (GAM). Returns
the estimated curve with 95\% confidence intervals and a ggplot2 object.
}
\examples{
set.seed(123)
n   <- 600
age <- rnorm(n, 35, 10)
# Continuous dose: hours of training (0-40)
dose <- pmax(0, 10 + 3 * (age - 35) / 10 + rnorm(n, 0, 8))
y    <- 2000 + 80 * dose - 1.5 * dose^2 + 50 * age + rnorm(n, 0, 500)
df   <- data.frame(earnings = y, training = dose, age = age)

result <- dose_response_curve(
  data       = df,
  outcome    = "earnings",
  treatment  = "training",
  covariates = "age",
  n_bins     = 15,
  method     = "ols"
)
result$plot

}
\references{
Hirano, K., & Imbens, G. W. (2004). The propensity score with continuous
treatments. In A. Gelman & X.-L. Meng (Eds.), \emph{Applied Bayesian Modeling
and Causal Inference from Incomplete-Data Perspectives}. Wiley.
}
