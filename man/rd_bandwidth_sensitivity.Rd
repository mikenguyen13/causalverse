% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd_bandwidth_sensitivity.R
\name{rd_bandwidth_sensitivity}
\alias{rd_bandwidth_sensitivity}
\title{RD Bandwidth Sensitivity Analysis}
\usage{
rd_bandwidth_sensitivity(
  data,
  y,
  x,
  c = 0,
  bw_multiples = seq(0.5, 2, 0.1),
  kernel = "tri",
  p = 1,
  conf_level = 0.95,
  theme_use = causalverse::ama_theme()
)
}
\arguments{
\item{data}{A data frame containing the outcome and running variable.}

\item{y}{Character string. Name of the outcome variable.}

\item{x}{Character string. Name of the running variable.}

\item{c}{Numeric. The cutoff value for the RD design. Default is \code{0}.}

\item{bw_multiples}{Numeric vector. Multipliers applied to the MSE-optimal
bandwidth from \code{rdrobust}. Default is \code{seq(0.5, 2, 0.1)}.}

\item{kernel}{Character string. Kernel function for \code{rdrobust}.
Default is \code{"tri"} (triangular).}

\item{p}{Integer. Order of the local polynomial. Default is \code{1}.}

\item{conf_level}{Numeric. Confidence level for intervals. Default is
\code{0.95}.}

\item{theme_use}{A ggplot2 theme to apply to the plot. Default is
\code{causalverse::ama_theme()}.}
}
\value{
A list with components:
\describe{
\item{\code{results}}{A data frame with columns: \code{bandwidth},
\code{bw_multiple}, \code{estimate}, \code{std_error}, \code{ci_lower},
\code{ci_upper}, \code{p_value}, \code{n_left}, \code{n_right}.}
\item{\code{plot}}{A ggplot2 object showing estimates and CIs across
bandwidths.}
\item{\code{optimal_bw}}{The MSE-optimal bandwidth from \code{rdrobust}.}
}
}
\description{
Runs \code{\link[rdrobust]{rdrobust}} over a grid of bandwidths and plots
point estimates with confidence intervals against bandwidth values. This
helps assess how sensitive the RD treatment effect estimate is to the choice
of bandwidth.
}
\examples{
\dontrun{
set.seed(42)
n <- 1000
x <- runif(n, -1, 1)
y <- 3 + 2 * (x >= 0) + 0.5 * x + rnorm(n)
df <- data.frame(y = y, x = x)

result <- rd_bandwidth_sensitivity(
  data = df,
  y = "y",
  x = "x",
  c = 0,
  bw_multiples = seq(0.5, 2, 0.25)
)

result$plot
result$results
result$optimal_bw
}

}
\references{
Calonico, S., Cattaneo, M. D., and Titiunik, R. (2014). "Robust
Nonparametric Confidence Intervals for Regression-Discontinuity Designs."
\emph{Econometrica}, 82(6), 2295-2326.
}
