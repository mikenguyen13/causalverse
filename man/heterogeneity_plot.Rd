% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heterogeneity_plot.R
\name{heterogeneity_plot}
\alias{heterogeneity_plot}
\title{Treatment Effect Heterogeneity Visualization}
\usage{
heterogeneity_plot(
  data,
  cate_var = NULL,
  subgroup_var = NULL,
  moderator_var = NULL,
  outcome = NULL,
  treatment = NULL,
  weights = NULL,
  overall_effect = NULL,
  conf_level = 0.95,
  plot_type = c("forest", "scatter", "violin"),
  title = "Treatment Effect Heterogeneity"
)
}
\arguments{
\item{data}{A data frame.}

\item{cate_var}{Character. Name of the column containing estimated CATEs.
Required for CATE-based plots.}

\item{subgroup_var}{Character. Name of a discrete subgroup variable.
If provided, creates a forest-style subgroup HTE plot.}

\item{moderator_var}{Character. Name of a continuous moderator variable.
If provided, creates a scatter/LOESS plot of CATE vs. moderator.}

\item{outcome}{Character. Outcome variable name (for computing raw
subgroup effects when \code{cate_var} is not available).}

\item{treatment}{Character. Treatment variable name.}

\item{weights}{Numeric vector. Optional analytical weights.}

\item{overall_effect}{Numeric or \code{NULL}. Overall ATE to show as
reference line. If \code{NULL}, estimated from data.}

\item{conf_level}{Numeric. Confidence level. Default \code{0.95}.}

\item{plot_type}{Character. \code{"forest"} (default for subgroups),
\code{"scatter"} (for continuous moderator), or \code{"violin"}.}

\item{title}{Character. Plot title.}
}
\value{
A ggplot2 object.
}
\description{
Creates publication-ready visualizations for treatment effect heterogeneity
(HTE) across subgroups or continuous moderators. Supports forest plots,
scatter plots of CATE vs. covariates, and marginal effects plots.
}
\examples{
\dontrun{
library(grf)
n <- 500; p <- 5
X <- data.frame(matrix(rnorm(n * p), n, p))
W <- rbinom(n, 1, 0.5)
Y <- pmax(X[, 1], 0) * W + rnorm(n)
cf <- causal_forest(as.matrix(X), Y, W)
df <- cbind(X, cate = predict(cf)$predictions)

# Scatter: CATE vs covariate
heterogeneity_plot(df, cate_var = "cate", moderator_var = "X1",
                   plot_type = "scatter")

# Subgroup forest plot
df$subgroup <- ifelse(X[, 1] > 0, "High X1", "Low X1")
heterogeneity_plot(df, cate_var = "cate", subgroup_var = "subgroup")
}

}
