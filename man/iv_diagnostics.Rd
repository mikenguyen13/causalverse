% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv_diagnostics.R
\name{iv_diagnostics}
\alias{iv_diagnostics}
\title{Instrumental Variables Diagnostics}
\usage{
iv_diagnostics(model, data = NULL, alpha = 0.05)
}
\arguments{
\item{model}{A fitted IV model. Supported classes:
\itemize{
\item \code{ivreg} from the \pkg{ivreg} package.
\item \code{fixest} estimated via \code{feols(..., iv = ...)} from \pkg{fixest}.
}}

\item{data}{Data frame used to fit the model. Required for some \code{fixest}
diagnostics; can be \code{NULL} for \code{ivreg} models.}

\item{alpha}{Numeric. Significance level for pass/fail thresholds. Default
\code{0.05}.}
}
\value{
A named list:
\describe{
\item{\code{first_stage}}{Named numeric vector: F-statistic and degrees of
freedom.}
\item{\code{overid}}{Named numeric vector with Sargan statistic and p-value,
or \code{NULL} if exactly identified.}
\item{\code{endogeneity}}{Named numeric vector with Wu-Hausman statistic and
p-value.}
\item{\code{summary_df}}{Data frame summarising all diagnostics with columns
\code{test}, \code{statistic}, \code{p_value}, \code{threshold}, \code{pass}.}
\item{\code{plot}}{A ggplot2 object visualising the diagnostics.}
}
}
\description{
Accepts a fitted IV / 2SLS model and returns a comprehensive
list of diagnostic statistics, each flagged as passing or failing standard
thresholds. A visual bar chart compares observed test statistics against
critical values.
}
\details{
Comprehensive IV / 2SLS Diagnostics

Runs a standard battery of instrumental variables diagnostics on a fitted
\code{ivreg} or \code{fixest} 2SLS model: first-stage strength (F-statistic),
weak-instrument tests (Cragg-Donald / Kleibergen-Paap), overidentification
(Sargan-Hansen), and endogeneity (Wu-Hausman). Returns all results in a
tidy list, including a ggplot2 summary bar chart.
}
\examples{
set.seed(42)
n  <- 500
z  <- rnorm(n)            # instrument
x  <- 0.8 * z + rnorm(n) # endogenous regressor
y  <- 1.5 * x + rnorm(n) # outcome
df <- data.frame(y = y, x = x, z = z)

if (requireNamespace("ivreg", quietly = TRUE)) {
  m <- ivreg::ivreg(y ~ x | z, data = df)
  diag <- iv_diagnostics(m)
  print(diag$summary_df)
}

}
\references{
Stock, J. H., & Yogo, M. (2005). Testing for weak instruments in linear IV
regression. In D. W. K. Andrews & J. H. Stock (Eds.), \emph{Identification and
Inference for Econometric Models}. Cambridge University Press.

Kleibergen, F., & Paap, R. (2006). Generalized reduced rank tests using the
singular value decomposition. \emph{Journal of Econometrics}, 133(1), 97-126.
}
