% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balance_plot.R
\name{balance_plot}
\alias{balance_plot}
\title{Comprehensive Balance Plot Suite}
\usage{
balance_plot(
  data,
  treatment,
  covariates = NULL,
  data_adj = NULL,
  weights = NULL,
  threshold = 0.1,
  var_labels = NULL,
  show_density = TRUE,
  show_variance_ratio = FALSE,
  title = "Covariate Balance"
)
}
\arguments{
\item{data}{A data frame.}

\item{treatment}{Character. Name of the binary treatment variable (0/1).}

\item{covariates}{Character vector. Names of covariates to plot. Defaults
to all numeric variables except \code{treatment}.}

\item{data_adj}{A second data frame (e.g., post-matching) to overlay as
"adjusted" estimates. Optional.}

\item{weights}{Numeric vector. Case weights for the adjusted sample
(alternative to \code{data_adj}). Length must equal \code{nrow(data)}.}

\item{threshold}{Numeric. SMD threshold line. Default \code{0.1}.}

\item{var_labels}{Named character vector. Display names for covariates.}

\item{show_density}{Logical. Add density overlay panels. Default \code{TRUE}.}

\item{show_variance_ratio}{Logical. Add variance ratio panel. Default \code{FALSE}.}

\item{title}{Character. Overall plot title.}
}
\value{
A ggplot2 object (or patchwork figure if multiple panels).
}
\description{
Produces a multi-panel diagnostic figure comparing covariate distributions
between treatment and control groups. Combines a standardized mean difference
(SMD) dot plot, density overlays, and (optionally) a variance ratio panel.
Useful for assessing balance before and after matching or weighting.
}
\examples{
set.seed(42)
n <- 300
df <- data.frame(
  treat  = rbinom(n, 1, 0.5),
  age    = rnorm(n, 40, 10),
  income = rnorm(n, 50000, 15000),
  female = rbinom(n, 1, 0.5)
)
# Introduce imbalance
df$age[df$treat == 1] <- df$age[df$treat == 1] + 5

balance_plot(df, treatment = "treat",
             covariates = c("age", "income", "female"))

}
