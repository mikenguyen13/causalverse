% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_coef_comparison.R
\name{plot_coef_comparison}
\alias{plot_coef_comparison}
\title{Compare Treatment Effect Estimates Across Models}
\usage{
plot_coef_comparison(
  models,
  coef_name = NULL,
  conf_level = 0.95,
  title = "Treatment Effect Comparison",
  xlab = "Estimate"
)
}
\arguments{
\item{models}{A named list of model objects (from \code{lm}, \code{fixest::feols},
etc.) or a data frame with columns \code{model}, \code{estimate},
\code{std_error}.}

\item{coef_name}{Character string. The name of the coefficient to extract
from each model. Required when \code{models} is a list of model objects.}

\item{conf_level}{Numeric. Confidence level for intervals. Default is 0.95.}

\item{title}{Character string. Plot title. Default is \code{"Treatment Effect Comparison"}.}

\item{xlab}{Character string. X-axis label. Default is \code{"Estimate"}.}
}
\value{
A ggplot2 object showing a forest plot.
}
\description{
Creates a forest plot comparing point estimates and confidence intervals
from multiple models or specifications. Useful for showing robustness of
causal estimates across different identification strategies.
}
\examples{
\dontrun{
m1 <- lm(mpg ~ am, data = mtcars)
m2 <- lm(mpg ~ am + wt, data = mtcars)
m3 <- lm(mpg ~ am + wt + hp, data = mtcars)

plot_coef_comparison(
  models = list("Bivariate" = m1, "With Weight" = m2, "Full" = m3),
  coef_name = "am"
)
}

}
